{"name": "command.declared", "payload": {"name": "capture", "description": "Capture a task onto the board (starts in Now). Joins argv into the task text. Emits task.captured.", "params": {"text": "string"}, "event": {"name": "task.captured", "fields": {"text": "string"}}, "implementation": "#!/usr/bin/env python3\nimport sys, json\ntext = \" \".join(sys.argv[1:]).strip() or \"(empty task)\"\nprint(json.dumps({\"name\": \"task.captured\", \"payload\": {\"text\": text}}))\n", "examples": [{"args": ["call", "the", "dentist"], "expect_contains": ["task.captured", "call the dentist"]}]}}
{"name": "command.declared", "payload": {"name": "move", "description": "Move a task to a lane (now|this_week|waiting|done; friendly spellings normalized). argv: id then lane. Emits task.moved.", "params": {"task": "int", "lane": "string"}, "event": {"name": "task.moved", "fields": {"task": "int", "lane": "string"}}, "implementation": "#!/usr/bin/env python3\nimport sys, json\nargs = sys.argv[1:]\ntask = args[0] if args else \"\"\nlane = \" \".join(args[1:]).strip().lower() or \"now\"\nALIAS = {\"this week\":\"this_week\",\"thisweek\":\"this_week\",\"todo\":\"now\",\"inbox\":\"now\",\n         \"today\":\"now\",\"complete\":\"done\",\"completed\":\"done\",\"finished\":\"done\",\n         \"blocked\":\"waiting\",\"later\":\"waiting\"}\nlane = ALIAS.get(lane, lane)\nif lane not in (\"now\",\"this_week\",\"waiting\",\"done\"): lane = \"now\"\ntry: task = int(task)\nexcept (ValueError, TypeError): pass\nprint(json.dumps({\"name\": \"task.moved\", \"payload\": {\"task\": task, \"lane\": lane}}))\n", "examples": [{"args": ["5", "waiting"], "expect_contains": ["task.moved", "5", "waiting"]}, {"args": ["3", "this", "week"], "expect_contains": ["this_week"]}]}}
{"name": "command.declared", "payload": {"name": "edit", "description": "Rename a task. argv: id then the new text (joined). Emits task.edited; the board shows the latest text.", "params": {"task": "int", "text": "string"}, "event": {"name": "task.edited", "fields": {"task": "int", "text": "string"}}, "implementation": "#!/usr/bin/env python3\n# edit \u2014 rename a task. argv: task id then the new text (joined). Emits task.edited.\nimport sys, json\nargs = sys.argv[1:]\ntask = args[0] if args else \"\"\ntry: task = int(task)\nexcept (ValueError, TypeError): pass\ntext = \" \".join(args[1:]).strip() or \"(empty task)\"\nprint(json.dumps({\"name\": \"task.edited\", \"payload\": {\"task\": task, \"text\": text}}))\n", "examples": [{"args": ["5", "new", "title"], "expect_contains": ["task.edited", "5", "new title"]}]}}
{"name": "command.declared", "payload": {"name": "drop", "description": "Delete a task (tombstone; the log keeps history). argv[0] is the id. Emits task.dropped; the board hides it.", "params": {"task": "int"}, "event": {"name": "task.dropped", "fields": {"task": "int"}}, "implementation": "#!/usr/bin/env python3\n# drop \u2014 delete a task (a tombstone; the log keeps the history). argv[0] is the\n# task id. Emits task.dropped; the board hides dropped tasks.\nimport sys, json\nargs = sys.argv[1:]\nt = args[0] if args else \"\"\ntry: t = int(t)\nexcept (ValueError, TypeError): pass\nprint(json.dumps({\"name\": \"task.dropped\", \"payload\": {\"task\": t}}))\n", "examples": [{"args": ["5"], "expect_contains": ["task.dropped", "5"]}]}}
{"name": "projector.declared", "payload": {"name": "board", "description": "Now/This week/Waiting/Done board. Folds task.captured/moved/edited/dropped. Each card has a rename (edit) form, move buttons (/run/move), and a delete button (/run/drop); a capture box at top (/run/capture). Bare semantic HTML, kernel class vocabulary.", "consumes": ["task.captured", "task.moved", "task.edited", "task.dropped"], "implementation": "#!/usr/bin/env python3\n# board \u2014 Now/This week/Waiting/Done, replayed from the log. Folds task.captured\n# (id = seq, starts Now), task.moved (lane), task.edited (text), task.dropped\n# (hide). Each card: rename (edit), move buttons, delete. Bare semantic HTML.\nimport sys, json\nfrom html import escape\nLANES = [(\"now\",\"Now\"),(\"this_week\",\"This week\"),(\"waiting\",\"Waiting\"),(\"done\",\"Done\")]\ntasks, order = {}, []\nfor line in sys.stdin:\n    line = line.strip()\n    if not line: continue\n    try: e = json.loads(line)\n    except Exception: continue\n    n = e.get(\"name\"); p = e.get(\"payload\", {}) or {}\n    if n == \"task.captured\":\n        sid = e.get(\"seq\")\n        tasks[sid] = {\"text\": p.get(\"text\",\"\"), \"lane\": \"now\",\n                      \"at\": (e.get(\"occurred_at\",\"\") or \"\")[:16].replace(\"T\",\" \"), \"dropped\": False}\n        order.append(sid)\n    elif n in (\"task.moved\",\"task.edited\",\"task.dropped\"):\n        t = p.get(\"task\")\n        try: t = int(t)\n        except (ValueError, TypeError): pass\n        if t in tasks:\n            if n == \"task.moved\": tasks[t][\"lane\"] = p.get(\"lane\",\"now\")\n            elif n == \"task.edited\": tasks[t][\"text\"] = p.get(\"text\", tasks[t][\"text\"])\n            elif n == \"task.dropped\": tasks[t][\"dropped\"] = True\nby_lane = {k: [] for k,_ in LANES}\nfor sid in order:\n    t = tasks[sid]\n    if t[\"dropped\"]: continue\n    by_lane.get(t[\"lane\"], by_lane[\"now\"]).append((sid,t))\nprint(\"<!DOCTYPE html>\")\nprint(\"<html><head><meta charset='utf-8'><title>board</title></head><body>\")\nprint(\"<h1>board</h1>\")\nprint(\"<form method='post' action='/run/capture'><input name='text' placeholder='capture a task and press enter...'><button>capture</button></form>\")\nactive = sum(1 for sid in order if not tasks[sid][\"dropped\"] and tasks[sid][\"lane\"] != \"done\")\nprint(\"<p class='muted'>%d open \u00b7 %d done</p>\" % (active, len(by_lane[\"done\"])))\nfor key,label in LANES:\n    items = by_lane[key]\n    print(\"<h2>%s <span class='muted'>(%d)</span></h2>\" % (escape(label), len(items)))\n    if not items:\n        print(\"<p class='muted'>\u2014</p>\"); continue\n    for sid,t in items:\n        print(\"<div class='card'>\")\n        print(\"<form method='post' action='/run/edit'>\"\n              \"<input type='hidden' name='task' value='%s'>\"\n              \"<input name='text' value='%s'>\"\n              \"<button class='secondary'>save</button></form>\" % (sid, escape(t[\"text\"], quote=True)))\n        print(\"<div class='row'>\")\n        for dk,dl in LANES:\n            if dk == key: continue\n            btn = \"\u2713 Done\" if dk == \"done\" else dl\n            print(\"<form method='post' action='/run/move'>\"\n                  \"<input type='hidden' name='task' value='%s'>\"\n                  \"<input type='hidden' name='lane' value='%s'>\"\n                  \"<button class='secondary'>%s</button></form>\" % (sid, dk, escape(btn)))\n        print(\"<form method='post' action='/run/drop'>\"\n              \"<input type='hidden' name='task' value='%s'>\"\n              \"<button class='danger'>\u2715</button></form>\" % sid)\n        print(\"</div>\")\n        print(\"<div class='muted'>#%s \u00b7 %s</div>\" % (sid, escape(t[\"at\"])))\n        print(\"</div>\")\nprint(\"</body></html>\")\n", "examples": [{"note": "edit renames; drop hides and lowers the open count", "events": [{"name": "task.captured", "seq": 2, "payload": {"text": "keep me"}}, {"name": "task.captured", "seq": 3, "payload": {"text": "remove me"}}, {"name": "task.edited", "payload": {"task": 2, "text": "kept and edited"}}, {"name": "task.dropped", "payload": {"task": 3}}], "expect_contains": ["kept and edited", "1 open"]}]}}
